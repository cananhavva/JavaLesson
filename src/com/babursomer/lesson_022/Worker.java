package com.babursomer.lesson_022;

public class Worker extends Employee {
	
	public Worker(long identityNumber, Gender gender, String firstName, String lastName, double salary,
			boolean fullTime) {
		super(identityNumber, gender, firstName, lastName, salary, fullTime);
	}
	
}